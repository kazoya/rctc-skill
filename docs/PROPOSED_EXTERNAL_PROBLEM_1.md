# Proposed external developer problem #1 (not launched)

## Problem
A maintainer wants a reversible README note linking a tool’s quickstart and ethical star/contribute CTA — without inventing project metrics or auto-deploying.

## Success evidence
- Ledger v2 with canonical ids, adapter hash, permissions granted
- Exactly one file changed under declared `write_paths`
- Verify step records command + exit code
- `verified_claim` is not marketed as stronger than evidence

## Non-goals
Network, git_write, deploy, secrets, Project1 purchasing changes
