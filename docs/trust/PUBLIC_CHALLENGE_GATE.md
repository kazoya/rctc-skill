# Public challenge gate (NOT launched)

Do **not** open the broad “Give RCTC one developer problem” campaign until:

1. Canonical identity ambiguity solved (`canonical.json`)
2. Adapter mapping declarative (manifests)
3. Permission declarations exist
4. Evidence ledger v2 works
5. Project1 canary passes
6. One external-style adapter installable via SDK without editing `engine.js`

Then: **two** real developer problems with clean ledgers → only then broader growth.

## Proposed external developer problem #1 (draft only)
**Title:** “Add a README support/quickstart block without inventing metrics”  
**Repro:** Any public repo with `README.md` + a single source-of-truth data/brain file  
**Expected:** inspect → declarative build adapter → verify marker → ledger v2  
**Not in scope:** deploy, git push, secrets
