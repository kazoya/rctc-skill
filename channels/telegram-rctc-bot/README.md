# Telegram RCTC Bot (scaffold)

Non-secret scaffold. Set `TELEGRAM_BOT_TOKEN` in env before run.

```bash
cd channels/telegram-rctc-bot
npm install
cp .env.example .env   # add token
npm start
```

Commands: /start /packages /cert /support /prompt
