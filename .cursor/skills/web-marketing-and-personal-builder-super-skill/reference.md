# Reference — stack, pages, honesty

## Stack (default production pattern)

- Next.js 16 App Router, React 19, Tailwind 4, shadcn
- `lang="ar"` `dir="rtl"` (bilingual OK if brand needs EN)
- Local Arabic font (e.g. Droid Arabic Kufi under `app/fonts/`)
- `qrcode.react`, `sonner`, `recharts` as needed
- Assessment / draft state in `localStorage` only unless user asks for a backend

Copy structure from a living reference — **style, not product content**:

| Reference | Best for |
|-----------|----------|
| `C:\almajjarra` | Field / irrigation / human valve decision |
| `C:\Factories\Banoon` | Retail / schools / contest engine |
| `C:\Factories\Muqasa-jo` | Marketplace / auction **marketing** concept |
| `C:\Factories\VStrata` | Calm B2B executive proposal tone |

## Config (`lib/config.ts`)

Priority for WhatsApp: user → spreadsheet if pointed → Contact page.  
Email: user → visible on site → never invent.  
Geo/hours: as published, else «غير منشور».

## What changes vs what does not

| Changes | Does not change |
|---------|-----------------|
| Colors from logo | Source honesty or apology |
| Catalog from site | Demo data badges |
| Interactive engine | Human approval before sensitive act |
| Sales scripts / audience | “Not an official system” footer |
| Parent company page if real | Default developer QR unless overridden |

## Interactive engines (pick ONE)

| Business | Engine |
|----------|--------|
| Field / irrigation | Duration suggestion — does not open a valve |
| Schools / retail | Points contest — does not announce prize alone |
| B2B orders | WhatsApp capture → pricing queue |
| Tenders / ICT | Deal board — draft only; send after approval |
| Food / claims | Ingredient claim gated on lab file |

## Typical routes (Arabic marketing concept)

`/` `overview` `products` `workflow` `bottlenecks` `automation` `ai` `crm` `dashboard` `roi` `architecture` `pilot` `assessment` `assessment/results` `readiness` `contact` + 1–2 differentiator pages.

Personal brand sites may shrink to: `/` `work` `offer` `proof` `contact`.

## Smart Factory 4.0 note

PLC / Profinet demo lines are **not** the sales platform. Inject only when the industry truly needs it (`_kit/scripts/inject-smart-factory.ps1`). Food/retail: traceability/quality analogs only. Auction marketing: skip 4.0.

## GitHub / Vercel defaults (portfolio)

- GitHub user: `kazoya`
- Vercel team: `muqasa` (`team_33SV2q7GqfsqIuQ9eJ9jfE1b`)
- Project name: lowercase repo name
